services:
  zuckerbot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./scripts:/app/scripts:ro
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
